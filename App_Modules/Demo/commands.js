define(['module', 'jquery'], function (module, $) {

    module.exports = function demoCommands(params) {
        if (!(this instanceof demoCommands))
            return new demoCommands(params);

        var self = this;

    };

});