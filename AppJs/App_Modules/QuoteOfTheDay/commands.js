define(['module', 'jquery'], function (module, $) {

    module.exports = function quoteCommands(params) {
        if (!(this instanceof quoteCommands))
            return new quoteCommands(params);

        var self = this;

    };

});